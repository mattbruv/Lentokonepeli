export const U16_MAX = 0xffff;
export const U8_MAX = 0xff;

export const ROTATION_DIRECTIONS = 256;

export enum Team {
  Spectator,
  Centrals,
  Allies
}

export enum Terrain {
  Normal,
  Desert
}
